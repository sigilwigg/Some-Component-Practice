// ========== [///// USER CLASS /////] ==========
class User {
    static async logout(user) {
        // ----- logout this user -----
        // API.logout(user)
        // API.clearToken();
    }

    static async syncUserToLocalStorage(user) {

    }
}